psycopg2-binary
numpy==1.26.4
faiss-cpu==1.12.0
openai==1.107.1
sentence-transformers==4.1.0
transformers==4.47.0
torch==2.6.0
scikit-learn==1.6.0
pandas==2.2.3
faiss-gpu-cu11==1.12.0  # GPU version of faiss, requires CUDA 11

# Optional / environment-specific (only install if you need them):
# torch (used by sentence-transformers) - left unpinned because it depends on platform
# transformers