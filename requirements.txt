psycopg2==2.9.10
numpy==1.26.4
faiss-cpu==1.12.0
openai==1.107.1
sentence-transformers==4.1.0
scikit-learn==1.6.0
pandas==2.2.3

# Optional / environment-specific (only install if you need them):
# torch (used by sentence-transformers) - left unpinned because it depends on platform
# transformers