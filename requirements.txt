--extra-index-url https://download.pytorch.org/whl/cpu

# --- Core / DB ---
psycopg2-binary
sqlalchemy>=2.0,<3.0        # confirm this matches what DIContainer actually uses
pydantic>=2.0,<3.0          # needed for .model_dump() used in make_json_serializable

# --- Numerics / vector search ---
numpy==1.26.4
faiss-cpu==1.12.0

# --- LLM providers ---
openai==1.107.1
anthropic>=0.40.0
google-genai>=0.3.0
click>=8.1.0                 # required by rag_schema_matcher_mistral.py (from click import prompt)

# --- Embeddings / ML ---
sentence-transformers==4.1.0
transformers==4.47.0
torch==2.6.0+cpu             # CPU-only wheel via --extra-index-url above; avoid pulling ~800MB CUDA build
scikit-learn==1.6.0
pandas==2.2.3

# --- Testing ---
pytest>=8.0.0
pytest-cov                   # optional but matches your -v --tb=short pytest usage pattern

#faiss-gpu-cu11==1.12.0